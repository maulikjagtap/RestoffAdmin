<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.design.restoffowner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".User_RegistionActivity"
            android:theme="@style/NoActionbar" />
        <activity
            android:name=".User_loginactivity"
            android:theme="@style/NoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ForgotPassword_Activity"
            android:theme="@style/NoActionbar" />
        <activity
            android:name=".Userpicandotherdata"
            android:theme="@style/NoActionbar" />
        <activity android:name=".UserProfile" />
        <activity android:name=".UpdateStationiteam" />
        <activity android:name=".Basicintro"></activity>
    </application>

</manifest>